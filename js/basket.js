/* class Modal {
  constructor({
    PageElems,
    modalElems,
  }) {
    this.elements = {
      PageElems,
      modalElems,
    }
    }
    this.elements.PageElems.forEach(this.hendlerModal, this);
    this.elements.modalElems.forEach(this.basketChang, this);
    
}



class



const modal = new Modal({
  PageElems: [{
    images: '.page-img',
    title: '.card-title',
    input: '.counter-group .counter-input',
    button:'.card-counter-button'
}],
  modalElems: [
    {
      name: '.modal-input-text-name',
      color: '.modal-color .color-label',
      images:'.modal-img',
      button: '.modal-button',
      input: '.modal .counter-input'
    }
  ]
}) */